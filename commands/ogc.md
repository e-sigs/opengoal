---
description: Create a new opengoal list (and switch to it)
agent: general
subtask: false
argument-hint: <list-name>
---

!`~/.config/opencode/skills/goal-tracker/goals list-create $ARGUMENTS`

Show the output above as-is. If no name was provided, briefly tell the user the usage is `/ogc <list-name>`.
